/**
 * Display name for project threads when `projects.assignee` is not set yet.
 * Customers must use this exact label when starting a conversation via the API
 * for unassigned projects; it is surfaced in conversation options and deep links.
 */
export const DEFAULT_TEAM_CONTACT_NAME = 'Your Superwork team';

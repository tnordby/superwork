# Superwork Phase 1 Cursor Pack

This pack includes:
- Cursor rule files in `.cursor/rules/`
- product and architecture docs in `docs/`
- `AGENTS.md` for role-based prompting

Suggested use:
1. Copy `.cursor/` into your repo root
2. Copy `docs/` into your repo
3. Use the docs as shared context
4. Use the `.mdc` files to steer Cursor behavior consistently
